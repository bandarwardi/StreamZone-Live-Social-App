Continue building the StreamZone frontend prototype. The core screens exist but several are visually complete while functionally incomplete, and the file structure needs correction. Do NOT touch backend/API logic — this is still a UI-only prototype with mock data and local state.

## PRIORITY 1 — Add missing screens

### Beauty Filter Panel + Go Live pre-stream flow
- Add a new "Go Live" pre-stream screen shown before entering LiveRoom in broadcast mode, with a live camera preview area (placeholder image/video is fine)
- Add a Beauty Filter bottom sheet/panel triggered from this screen with:
  - Sliders (0-100) for: Smooth Skin, Whiten, Face Slim, Eye Enlarge — store values in local state, no real image processing needed
  - A horizontal scrollable row of filter/preset thumbnails (color filter presets + AR sticker placeholders) — static preview images are fine, just make selection state work (highlight selected)
- Add a "Start Live" button on this screen that navigates into the existing LiveRoom

### Splash Screen
- Replace the default Expo splash with a custom StreamZone-branded splash screen (logo + app name, matching the app's color palette)
- It should transition automatically after ~1.5s into Auth (if not "logged in") or Home (if a mock "logged in" flag is true in the store)

## PRIORITY 2 — Make existing interactions actually functional (not just visual)

### PK Battle
- Replace the static score bar percentages with real state: track separate score counters for each side in the store (or local state), increment the relevant side's score when a gift is sent during PK mode, and recompute the bar width from actual scores
- Replace the static "02:04:36" timer text with a real countdown (useEffect + setInterval, format mm:ss), and trigger the Winner overlay automatically when it reaches 0 based on whichever side has the higher score
- Add a confetti animation on the Winner screen (simple Reanimated-based particle/confetti effect is fine, doesn't need a library)

### Voice Room
- Make the mic button toggle a real "muted" state (already have `muted` state pattern used elsewhere — reuse it) and reflect it visually (icon + reduced opacity on own avatar ring)
- Add a working Raise-hand toggle state (icon changes, and add the user's own avatar to a "waiting to speak" indicator)
- Add a simple room background theme picker (3-4 preset gradient/color backgrounds, tap to apply to the room background)
- Make the active-speaker pulse animation cycle between a couple of seats automatically to simulate live conversation (existing Animated logic can just cycle a `activeSpeaker` index)

### Multi-Live Room
- Add working per-seat controls: tapping an occupied seat (as host) opens a small action sheet with Mute / Remove guest — update local seat state accordingly
- Add a working mute/camera toggle icon per seat that updates local state and reflects visually

### Live Streaming Room (broadcast/viewer)
- Add a viewer avatars row (3-4 stacked small avatar bubbles + "+N" count) in the top-right area, using existing mock avatar data
- Make the chat feed auto-scroll and fade/remove the oldest message when new ones are added (cap at ~4-5 visible messages)
- Wire up the "More options" (...) button to open a simple action sheet (Report, Block, Share — UI only)
- Wire up the Share button to open a native Share sheet (expo-sharing or RN Share API) with placeholder share text/link

### Messages
- Make each conversation in the list open its OWN chat thread (pass conversation id into ChatScreen instead of always opening the same static one) with that conversation's mock messages
- Mark a conversation as read (remove unread badge) once its chat is opened, using store state

## PRIORITY 3 — Structural refactor

### Break the single StreamZoneApp.tsx into Expo Router file-based screens
- Move each screen into its own file under `app/`, following Expo Router conventions (e.g. `app/(tabs)/index.tsx` for Home, `app/(tabs)/feed.tsx`, `app/(tabs)/messages.tsx`, `app/(tabs)/profile.tsx`, `app/room/[mode].tsx` for LiveRoom/PK/Multi/Voice, `app/wallet.tsx`, `app/auth.tsx`, `app/setup.tsx`, `app/chat/[id].tsx`, etc.)
- Keep shared UI primitives (Icon, Avatar, PillButton, Header, SectionTitle, Logo, BottomNav) as reusable components in `components/`
- Replace the current local-state screen-switching logic with real Expo Router navigation (`router.push`, `router.back`, dynamic route params for room mode / chat id)
- Keep the Zustand store (`useStreamStore`) as the single source of truth for theme, coins, diamonds, following, likes — this should NOT change

### Fix known issues while refactoring
- Fix the leftover placeholder default value `useState('hh h')` in the profile setup name field — should default to an empty string
- Add clear `// TODO:` comments at every point a real integration will be needed: API calls, authentication, payment/wallet transactions, live streaming/WebRTC (Agora), and chat backend (Socket.io) — so backend integration is a clear checklist later

## What NOT to do
- Do not implement real Agora/WebRTC, real Socket.io, real payments, or real authentication — all of the above must remain mock/local-state only
- Do not add TanStack Query real network calls — if you set up mock query hooks, they should read from local mock-data files, not real endpoints

Work through the priorities in order (1, then 2, then 3) and confirm what's been completed at each stage before moving to the next.